import Component from "../Component/Component.js";

class Aside extends Component {
  constructor(parentElement: HTMLElement, className: string) {
    super(parentElement, className, "aside");
  }

  render(): void {
    super.render();
  }
}

export default Aside;
